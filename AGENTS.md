# ReportBook 작업 지침

## 폴더별 역할과 소유권

- `src/client`: 브라우저에서 실행되는 TypeScript, HTML, CSS와 정적 자산을 소유한다.
- `src/server`: HTTP API, SQLite 연결, 데이터 저장 규칙을 소유한다.
- `src/scripts`: 빌드와 개발 보조 스크립트를 소유한다.
- `src/test`: 자동화 테스트와 회귀 검증을 소유한다.
- `src/data`: 실행 중 생성되는 SQLite 파일을 보관한다. 시안 및 샘플 레코드를 소스에 포함하지 않는다.
- `src/dist`: 빌드 결과물이다. 원본 수정 대신 빌드 명령으로 갱신한다.
- `project/_design`: 디자인 참고 자료이며 사용자가 별도로 요청하지 않으면 수정하지 않는다.

## 워크플로

### 소스 또는 자산 변경

- 트리거 문구: 사용자가 "수정해줘", "구현해줘", "제거해줘" 또는 "삭제해줘"라고 요청한 경우다.
- 실행 순서: 대상의 정규 경로 확인 → 참조 검색 → 원본 변경 → UTF-8 검사 → 관련 테스트 → 결과 보고 순서로 진행한다.
- 중지 조건: 대상이 여러 경로로 해석되거나, 요청 범위 밖 파일 변경이 필요하거나, 필수 파일에 접근할 수 없으면 중지한다.
- 오류 처리: 실패한 명령과 영향을 받은 파일을 확인하고 추가 쓰기를 중단한 뒤 원인, 증거, 다음 조치를 보고한다.
- 재실행 조건: 원인이 제거되었거나 사용자가 필요한 입력을 제공한 경우 처음 실패한 단계부터 다시 실행한다.
- 중복 방지: 동일 경로에 대한 변경이 이미 적용됐는지 먼저 확인하고, 적용된 작업은 반복하지 않는다.

### 빌드 및 테스트

- 트리거 문구: 소스 변경이 완료됐거나 사용자가 "테스트해줘", "빌드해줘"라고 요청한 경우다.
- 실행 순서: 패키지 스크립트 확인 → 테스트 실행 → 빌드 실행 → 생성물 확인 순서로 진행한다.
- 중지 조건: 의존성 또는 실행 명령이 없고 안전한 로컬 대체 절차도 없으면 중지한다.
- 오류 처리: 첫 실패의 로그를 보존하고 원본 소스와 생성물 중 어느 단계가 실패했는지 구분해 보고한다.
- 재실행 조건: 실패 원인을 수정한 뒤 실패한 검증부터 다시 실행한다.
- 중복 방지: 같은 소스 상태에서 성공한 명령은 추가 변경이 없으면 반복하지 않는다.

### 데이터베이스 변경

- 트리거 문구: 사용자가 "DB", "데이터베이스" 또는 "SQLite" 변경을 요청한 경우다.
- 실행 순서: 대상 파일 확인 → 스키마와 초기화 코드 검사 → 변경 → 빈 데이터베이스 기준 테스트 → 영속화 경로 확인 순서로 진행한다.
- 중지 조건: 기존 사용자 데이터 삭제 가능성이 있는데 명시적 삭제 승인이 없으면 중지한다.
- 오류 처리: 데이터 파일을 보존하고 애플리케이션 초기화 로그와 테스트 결과를 보고한다.
- 재실행 조건: 백업 또는 삭제 범위가 확인되고 사용자가 필요한 결정을 제공한 경우 재실행한다.
- 중복 방지: 마이그레이션과 초기화 작업은 재실행해도 같은 결과가 되도록 작성한다.
